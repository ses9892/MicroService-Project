spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: user-service
#  profiles:
#    active: native
  # http://127.0.0.1:8888 에서 읽어오고자 하는 파일 ecommerce.yml 이름 명시

# spring-cloud-starter-config  , spring-cloud-starter-bootstrap 을 DI 하게 되면
# bootstrap.yml 파일의 우선순위가 application.yml 보다 높게 설정되어 현재 파일이 먼져 읽힌다.
